# Server Configuration
server.port=8080
server.address=0.0.0.0

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/medora?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Chandran@2006
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:your-secret-key-change-in-production}
jwt.expiration=86400000

# Agora Configuration
# IMPORTANT: Replace with your actual Agora credentials from https://console.agora.io
# Current credentials are invalid - create a new project
agora.app.id=${AGORA_APP_ID:YOUR_NEW_APP_ID_HERE}
agora.app.certificate=${AGORA_APP_CERTIFICATE:YOUR_NEW_APP_CERTIFICATE_HERE}
agora.token.expiration=3600
